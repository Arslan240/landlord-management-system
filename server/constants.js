const LEASE_PENDING = "pending"
const LEASE_ACCEPTED = "accepted"
const LEASE_REJECTED = "rejected"
const LEASE_CANCELLED = "cancelled"

module.exports = { LEASE_PENDING, LEASE_ACCEPTED, LEASE_REJECTED, LEASE_CANCELLED }
